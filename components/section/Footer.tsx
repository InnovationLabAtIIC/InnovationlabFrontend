'use client';

export default function Footer() {

    const LINKS = [
        { label: 'About', href: '#' },
        { label: 'Events', href: '#' },
        { label: 'Updates', href: '#' },
        { label: 'FAQs', href: '#' },
        { label: 'Contact', href: '#' },
    ];

    return (
        <footer className="">
        </footer>
    );
}